#include "object.h"

namespace CL {

	void Object::set(const RuntimeValue & key, RuntimeValue value) {

	}
	RuntimeValue &get(const RuntimeValue &) {
		TODO();
	}
	void set_named(const std::string &name, RuntimeValue v) {
		TODO();

	}
	RuntimeValue &get_named(const std::string &name) {
		TODO();
	}
}